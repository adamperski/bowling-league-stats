# Keep archived snapshots byte-for-byte as fetched (no EOL conversion).
data/raw/** -text

# Normalize line endings for source we edit.
*.ps1   text eol=crlf
*.md    text eol=lf
*.html  text eol=lf
*.json  text eol=lf
